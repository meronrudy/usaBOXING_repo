# usaBOXING

Sport-agnostic, actor-agnostic, `no_std`-first Rust platform with thin Apple hosts.

Non-negotiables:
- zero Python
- deterministic behavior
- `no_std` kernel by default
- C ABI + thin Swift wrapper
